<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="12dp"
    app:cardCornerRadius="20dp"
    app:cardElevation="6dp"
    android:backgroundTint="#FFFFFF">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">


        <!-- Foto -->
        <ImageView
            android:id="@+id/iv_task_photo"
            android:layout_width="90dp"
            android:layout_height="90dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_launcher_foreground"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <!-- Título -->
        <TextView
            android:id="@+id/tv_task_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="Tarea de Redes"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="#000000"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toEndOf="@id/iv_task_photo"
            app:layout_constraintEnd_toStartOf="@id/radio_group"
            android:layout_marginStart="16dp" />

        <!-- Fecha -->
        <TextView
            android:id="@+id/tv_task_due_date"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:text="15 de noviembre"
            android:textSize="14sp"
            android:textColor="#666666"
            app:layout_constraintTop_toBottomOf="@id/tv_task_title"
            app:layout_constraintStart_toEndOf="@id/iv_task_photo"
            app:layout_constraintEnd_toStartOf="@id/radio_group"
            android:layout_marginTop="4dp"
            android:layout_marginStart="16dp" />

        <!-- Curso -->
        <TextView
            android:id="@+id/tv_task_course"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Curso: Redes"
            android:textSize="14sp"
            android:textColor="#2167F3"
            android:background="#E3F2FD"
            android:paddingHorizontal="12dp"
            android:paddingVertical="6dp"
            app:layout_constraintTop_toBottomOf="@id/tv_task_due_date"
            app:layout_constraintStart_toEndOf="@id/iv_task_photo"
            android:layout_marginTop="8dp"
            android:layout_marginStart="16dp" />

        <!-- 2 RadioButtons -->
        <RadioGroup
            android:id="@+id/radio_group"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginEnd="16dp">

            <RadioButton
                android:id="@+id/rb_completed"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Hecha" />

            <RadioButton
                android:id="@+id/rb_not_completed"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="No hecha" />
        </RadioGroup>

        <!-- CheckBox: Entregada -->
        <CheckBox
            android:id="@+id/cb_delivered"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Entregada"
            android:textSize="14sp"
            app:layout_constraintTop_toBottomOf="@id/radio_group"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginTop="8dp"
            android:layout_marginEnd="16dp" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>